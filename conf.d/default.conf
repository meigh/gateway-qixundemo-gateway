server {
    listen 8080;

    # ========= Basic Auth（统一管 web + bot） =========
    auth_basic "Restricted";
    auth_basic_user_file /etc/nginx/.htpasswd;

    # ========= 路由拆分（按文件名顺序加载） =========
    include /etc/nginx/conf.d/routes/*.conf;
}
